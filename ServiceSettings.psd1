@{
	####
	
	ServiceAccount		= "ServiceUser"
	# User account that is required for changes to take effect. Otherwise, services run in test mode (no changes, pause after execution)
	
	ServiceHost			= "HOSTNAME"
	# Hostname that is required for changes to take effect. Otherwise, services run in test mode (no changes, pause after execution)
	
	ModulesPath 		= "C:\PSsvctask\Modules"
	# Where the PSsvctask modules live
	
	TasksPath 			= "C:\PSsvctask\Tasks"
	# Where the task scripts live

	####
}